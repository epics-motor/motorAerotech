# Use the following lines if motorAerotech was built inside motor
MOTOR=
-include $(MOTOR)/configure/RELEASE
# Use the following lines if motorAerotech was built outside motor
#!MOTOR_AEROTECH=
#!-include $(MOTOR_AEROTECH)/configure/RELEASE.local
